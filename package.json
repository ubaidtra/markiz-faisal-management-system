{
  "name": "faisal-quranic-center-management",
  "version": "1.0.0",
  "description": "Faisal, His Parents and Family's Qura'anic Memorization Center - School Management System",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && nodemon server.js",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install --production",
    "build:all": "npm run build:backend && npm run build",
    "vercel-build": "cd frontend && npm ci && npm run build",
    "start": "cd backend && npm start",
    "start:prod": "cd backend && NODE_ENV=production npm start",
    "deploy:prepare": "node scripts/deploy.js || powershell -ExecutionPolicy Bypass -File scripts/deploy.ps1"
  },
  "keywords": ["school", "management", "quranic", "center"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.2"
  }
}
